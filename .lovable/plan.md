Reiniciar o servidor de desenvolvimento para destravar o preview.

Passo único:
1. Executar `code--restart_dev_server` para reiniciar o Vite/TanStack Start dentro do sandbox sem recriar o ambiente.

Após o restart, o preview deve recarregar automaticamente na rota atual (`/`). Se ainda aparecer "Previewing last saved version", fazer um hard refresh (Ctrl+Shift+R) no painel de preview.